---
description: OmniDev trigger gate — load ONLY on /od prefix OR explicit @od skill attach. No session-context inference.
alwaysApply: true
---
# OmniDev Trigger Gate (Cursor)

→ Spec: `.cursor/skills/od/engine/trigger-gate.md`

## ACTIVATE — Signal A or B only

1. **Signal A**: Message starts with `/od` (or `$od`)
2. **Signal B**: User attached `@od` skill this turn (NOT `available_skills` listing alone). If unsure → do **not** activate.

On activate: Read SKILL.md → `engine/activation.md` §0–§6 → route. **Tool calls first.**

## DO NOT ACTIVATE

- Normal chat without `/od`/`$od` and without skill attach
- Bare `1` / `n` / `continue` — **NOT** workflow input
- Do NOT infer OmniDev from prior turns or `in_progress` on disk

If bare `n`/`1`/`continue`/`ad`/`re` looks like workflow intent, reply **one line only**:
`⚠️ OmniDev is not active. Send a full command, e.g. /od n. Resume with /od re.`

## Advance workflow (every typed step needs `/od`)

| Action | Command |
|--------|---------|
| Resume | `/od re` |
| Next phase | `/od n` |
| Revise | `/od ad` |
| End session | `/od x` |

Checkpoint: **must call** `AskQuestion` when tool exists → user picks UI **or** sends `/od` next message. If tool absent → clean `/od` table (never "reply 1/2/3"). **STOP & WAIT** (never auto-continue after pseudo-popup).
Phase 0 chat: ≤6 lines only; details → session-log.
